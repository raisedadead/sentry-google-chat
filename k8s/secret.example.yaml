# Template only — DO NOT commit real values. Create the real Secret out of band
# (kubectl create secret, sealed-secrets, or external-secrets), never in git.
#
#   kubectl create secret generic sentry-google-chat \
#     --from-literal=SENTRY_CLIENT_SECRET=... \
#     --from-literal=GCHAT_WEBHOOK_DEFAULT='https://chat.googleapis.com/v1/spaces/.../messages?key=...&token=...' \
#     --from-literal=GCHAT_ROUTES='{"api-prod":"https://chat.googleapis.com/v1/spaces/.../messages?key=...&token=..."}' \
#     --from-literal=SENTRY_DSN=...
apiVersion: v1
kind: Secret
metadata:
  name: sentry-google-chat
  labels:
    app: sentry-google-chat
type: Opaque
stringData:
  SENTRY_CLIENT_SECRET: replace-me
  GCHAT_WEBHOOK_DEFAULT: https://chat.googleapis.com/v1/spaces/SPACE_ID/messages?key=KEY&token=TOKEN
  GCHAT_ROUTES: '{}'
  SENTRY_DSN: ''
